public class BankService {
    int calculate(int point) {
        // 1 bal = 1 rub
        // 3000 rub
        int win = 3000;
        if (point == win) {
            return win;
        }
        int credit = 1;
        boolean profit = point == credit ;
        return point;
        return point;
    }
}

